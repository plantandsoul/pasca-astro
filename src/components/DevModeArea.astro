---
const isDevEnvironment = import.meta.env.NODE_ENV === 'development';
---

{isDevEnvironment && <pre><slot /></pre>}

<style>
  pre {
    border: 1px solid var(--clr-line);
    padding: 0.5rem;
  }
</style>
